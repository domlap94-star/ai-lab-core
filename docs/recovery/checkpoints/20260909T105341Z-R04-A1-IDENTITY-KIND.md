# R04-20260909T105341Z-A1-IDENTITY-KIND

- UTC: `2026-09-09T10:53:41Z`
- Pakiet/podetap: `R04 / A1 IDENTITY_KIND_FIX`
- Status: `IDENTITY_KIND_FIX_READY_FOR_REVIEW / NOT_DEPLOYED`; cały R04:
  `IN_PROGRESS`
- Branch/worktree: `recovery/next-stabil-repair-completion` /
  `C:\ai-lab-core-recovery`
- Parent: `10b6c4eafeb14fcba0bd17e5bad6f4753f8624c5`
- Source/test A1: `ec102a8dacac529c3ac5f6715c08e4dd3ac35020`
- Source baseline: `origin/main@483f9bf8b1a591ded8a42df5da87663c664ed5d4`
- Rescue: `5cd8f86e63e1ab829692ca2601096fd0c0d9d53a`, nieadoptowane

## Zakończone

- Odtworzono na rzeczywistym, niezmienionym module błąd review: identyczne
  etykiety `contract_version` dla workera i `schema_revision` dla Web błędnie
  dawały `VERIFIED`.
- W istniejącym walidatorze dodano skończoną mapę komponent → dozwolony
  `identity_kind`. API przyjmuje `contract_version`, DB `schema_revision`,
  buildy wymagają właściwego SHA-256, a backend/Supervisor/gateway/workery
  właściwego `git_sha`, `sha256` lub `repo_digest`.
- Zły rodzaj po dowolnej stronie daje `UNVERIFIED`; niezależna prawidłowo
  wykazana różnica zachowuje `MISMATCH` i nie ukrywa pól `unverified`.
- Nie zmieniono `/version`, publicznej allowlisty, stable/update manifestu,
  minimum/force-update, parsera klienta, modeli danych ani frontendu.

## Dowody bieżącego wykonania

- Moduł fail-before SHA-256:
  `99D634E7F357D4949118834B40918F083C8982BB99C97A21FDBAA79E111E8F2F`.
- Bezpośredni fail-before: poprawny fixture `VERIFIED`, brak workera
  `UNVERIFIED`, poprawna różnica SHA Windows `MISMATCH`, lecz zły rodzaj
  workera/Web błędnie `VERIFIED`; exit `0` procesu reprodukcji.
- Nowe testy przed fixem: `12 failed, 11 passed`, pytest exit `1`.
- Bezpośrednio po fixie: zły rodzaj workera expected/observed/both i Web daje
  `UNVERIFIED`; poprawny fixture nadal `VERIFIED`, różny prawidłowy SHA nadal
  `MISMATCH`; exit `0`.
- Focused: `23 passed`, exit `0`.
- Combined z `test_followup_chunk22_system_status.py` oraz
  `test_android_release_api_configuration.py`: `34 passed`, exit `0`.
- Python compile zmienionych plików: `2/2`, exit `0`.
- `git diff --check`: `PASS`.
- Obraz testowy: `next-stabil-r02-tests:pytest835-923dc559-fulltree`;
  Python `3.12.13`, source read-only, `--network none`, syntetyczne env/tmpfs,
  bez lifespan, DB i zewnętrznych usług.
- Pełne komendy i ograniczenia:
  `docs/recovery/R04_A1_VERSION_IDENTITY_EVIDENCE.md`.

## Runtime, skutki i granice

- Komparator porównuje prawidłowo opisane dane wejściowe; nie obserwuje sam
  plików ani procesów i nie dowodzi tożsamości wdrożonego runtime.
- Utworzone trwałe zasoby testowe: `0`; kontenery `--rm` po testach: `0`.
- Produkcja, DB, konfiguracja, modele, workery, kolejki, backupy, escrow i
  zasoby A2/A3/A4: bez operacji.
- Oryginalny dirty worktree pozostaje 6 modified + 197 untracked, staged 0,
  łącznie 203 pozycje; main i rescue niezmienione przez tę sesję.
- R03 pozostaje `WAITING_APPROVAL / WAITING_ESCROW_DECISION`.
- R04-A1 nie jest `ACCEPTED`; pozostałe podetapy R04 oraz R05–R24 nie zostały
  uruchomione.

Następna bezpieczna czynność: odbiór ograniczonej poprawki identity-kind przez
właściciela. Każdy następny podetap wymaga osobnej decyzji.

Poprzedni checkpoint:
`docs/recovery/checkpoints/20260909T092544Z-R04-A1-SOURCE.md`.
