# R05-A2 — checkpoint guardu ponowienia uploadu

- Checkpoint ID: `R05-20260911T232156Z-A2-UPLOAD-REPLAY`
- UTC: `2026-09-11T23:21:56Z`
- Repo / branch / worktree: `domlap94-star/ai-lab-core` /
  `recovery/next-stabil-repair-completion` / `C:\ai-lab-core-recovery`
- Wejście kontynuacji: `869bffc4f049889a8e20cda09b7da1552a23ee6d`
- A2 source poddane uwadze: `dc075426c233ac204cec144be04cb66e74349702`
- Source guardu objęte testem i opublikowane:
  `d1b0518ad9aefb5bc05cd89308de923ca54d2809`
- Poprzedni checkpoint:
  `docs/recovery/checkpoints/20260911T222308Z-R05-A2-UPLOAD-BOUNDARY.md`

## Faktycznie zakończono

- Zachowano odbiór R05-A1: source
  `d31e105427acd40733e91c4d4b46f0412b0f95ad`, evidence
  `05995ab2c87438c5786a9f874793e95d2bf5b296`,
  `SOURCE_AND_SYNTHETIC_TESTS_ACCEPTED / NOT_DEPLOYED`.
- Na rzeczywistych `VisionQueue`, `loadVerifiedInputs()` i
  `uploadVerifiedInputs()` deterministyczny fail-before odtworzył przeplot:
  marker utworzony między `recover()` i `_start()` dał drugi spawn i łącznie
  dwa fake uploady; dwa wejścia granicy również dały dwa fake uploady.
- Worker przejmuje lokalne prawo do pierwszego kontaktu przez wyłączne
  utworzenie markera V2 przed `setInputFiles`. Marker wiąże job, próbę i
  dokładny zestaw źródeł. Istniejący, przerwany, nieczytelny lub historyczny
  marker nie jest nadpisywany.
- Kolejka ponownie ocenia marker bezpośrednio przed spawnem. Marker powstały po
  recover daje `UPLOAD_UNCERTAIN`; drugi worker zatrzyma się także przy
  wyłącznym przejęciu, jeśli marker powstanie po rechecku kolejki.
- Potwierdzenie uploadu jest przypisywane tylko do właściwego attempt/job/source
  binding. Przerwanie po przejęciu pozostawia konserwatywny dowód możliwego
  kontaktu i nie przywraca automatycznego retry.

## Kontrole

| Kontrola | Wynik | Dowód |
|---|---|---|
| Fail-before replay na preimage | REPRODUCED: recover `spawns=1`, `fake_uploads=2`; parallel boundary `fulfilled=2`, `fake_uploads=2` | exit `0` testu reprodukcji; log SHA-256 `712A19BE5AB38060D28EAF57101B9564FFA7276E75610048541AFEDE3545E33F` |
| Pass-after recover race | PASS: `restarted_spawns=0`, łącznie `fake_uploads=1`, stan `UPLOAD_UNCERTAIN` | `test_upload_replay.js`, source `d1b0518...` |
| Pass-after dwa wejścia worker boundary | PASS: `fulfilled=1`, `rejected=1` (`UPLOAD_HANDOFF_ALREADY_EXISTS`), `fake_uploads=1` | ten sam test; rzeczywista funkcja workera |
| Backend chain | PASS: `76 passed`, exit `0` | przypięty obraz R02; log SHA-256 `7EA2BD866BAC0F1F36302CA9A7627D8DF5D61075DEEFE88641361863ADA59028` |
| Node syntax i regresje | PASS: 5 `node --check` i 5 skryptów testowych, każdy exit `0` | Node `v24.18.0`; log SHA-256 `BCA01B5B9412DFA801D77A9A18FDA745EFACC0805EE030350D8AC0A04A2AF12C` |
| Browser / worker CLI / Supervisor HTTP / Temporary Chat / external upload | NOT_RUN / NOT_VERIFIED | poza zakresem zgody |
| Flutter/Web/APK/Android, modele, PostgreSQL, R03/R04 | NOT_RUN | brak zmian tych zakresów |

Testy backendu użyły `docker run --rm --network none --read-only`, read-only
source, tmpfs i wyłącznie syntetycznych ustawień. Testy Node podstawiły
`spawnWorker` oraz `setInputFiles`; nie wywołały browsera ani sieci. Wynik
`fake_uploads=1` nie jest dowodem realnego uploadu ani gwarancją exactly-once
zewnętrznej usługi lub utraty zasilania.

## Skutki i odtwarzalność

- Source zapisano osobnym commitem
  `d1b0518ad9aefb5bc05cd89308de923ca54d2809` i potwierdzono na zdalnej
  gałęzi recovery przed przygotowaniem tego checkpointu.
- Dowody kontynuacji są `LOCAL_ONLY` pod
  `C:\ai-lab-core-staging\recovery\R05_A2_UPLOAD_REPLAY_20260911T230541Z`;
  hashe i rozmiary znajdują się w
  `docs/recovery/R05_A2_LOCAL_EVIDENCE_MANIFEST.csv`.
- Powstał jeden efemeryczny kontener `docker run --rm`, tmpfs i syntetyczne
  pliki chain. Nie utworzono named volumes, sieci ani portów hosta.
- Nie uruchomiono aplikacji, normalnego dispatchera, Supervisor HTTP, worker
  CLI, browsera, profilu, Temporary Chat, modeli, Qdrant, Gmaila, n8n ani
  realnego eksportu. Produkcyjne DB/storage/spool i zachowane zasoby R03/R04
  nie były używane.
- Oryginalny worktree zachowuje 203 wpisy i staged `0`; main i rescue nie
  zostały zmienione ani adoptowane.
- Dokumentacyjne zmiany oraz ten checkpoint są odtwarzalne z commita, który je
  zawiera; jego przyszły SHA nie jest wpisywany do własnej treści.

## Stan i STOP

- R05-A1: `SOURCE_AND_SYNTHETIC_TESTS_ACCEPTED / NOT_DEPLOYED`.
- R05-A2: `UPLOAD_REPLAY_GUARD_READY_FOR_REVIEW / NOT_DEPLOYED`.
- Dowód lokalny: `OFFLINE_EXACT_BYTES_AND_AT_MOST_ONE_FAKE_UPLOAD_PASS`.
- Real browser/upload/external end-to-end: `NOT_VERIFIED`.
- R05 i R04: `IN_PROGRESS`.
- R03: `WAITING_APPROVAL / WAITING_ESCROW_DECISION`.
- Android: `DEFERRED_BY_OWNER / NOT_TESTED`; D-15/D-16 AI: `NOT_RUN`.
- R06–R24 nie zostały rozpoczęte.

Następny bezpieczny krok: właściciel przegląda i przyjmuje albo odrzuca source
`d1b0518ad9aefb5bc05cd89308de923ca54d2809` oraz ograniczony wynik A2. Realny
browser/upload smoke, rollout kolejki/workera i R06 wymagają osobnej zgody.
STOP.
