# R05-20260912T121418Z-A4-OPERATOR-START

- UTC: `2026-09-12T12:14:18Z`.
- Repo/worktree: `domlap94-star/ai-lab-core`, branch
  `recovery/next-stabil-repair-completion`, `C:\ai-lab-core-recovery`.
- Start local/remote HEAD:
  `1ae3f99edd5cb39b9cb795ba573878e9ded6b30e`; recovery clean, staged /
  unstaged / untracked `0 / 0 / 0`.
- Owner decision: R05-A3 evidence
  `1ae3f99edd5cb39b9cb795ba573878e9ded6b30e` for product functions
  `d1b0518ad9aefb5bc05cd89308de923ca54d2809` is accepted as
  `LOCAL_BROWSER_FILE_INPUT_ACCEPTED / TEST_ONLY / NOT_DEPLOYED`. R05-A4 is
  authorized only for source and isolated tests of an informed operator flow;
  A4 and the whole R05 are not accepted.
- Protected state: `origin/main@483f9bf8b1a591ded8a42df5da87663c664ed5d4`
  and rescue `5cd8f86e63e1ab829692ca2601096fd0c0d9d53a` are read-only. The original
  worktree remains at `72950657ac79b50d0afe72753632ba4cde810b95` with 203
  entries (6 modified + 197 untracked), staged 0.
- Preflight: remote recovery matched the expected SHA; no Docker container
  mounted recovery/A4 and the only matching process was the preflight shell
  itself. Git hooks are Git LFS only and no repository CI workflow was found.
- Existing flow: the admin-only candidate/approve/revoke routes persist and
  validate exact-byte bindings, but there is no authenticated endpoint that
  returns the staged final raster and no Flutter operator flow.
- Selected additive route:
  `GET /api/v1/documents/{document_id}/vision/export-approval/{analysis_job_id}/sources/{source_ref}/preview`.
  It must accept the candidate package/source hash, independently re-derive
  scope and bytes, read a bounded buffer once, return `no-store`, and perform
  no approval, background scheduling or external contact.
- Source/test allowlist before implementation:
  `backend/app/api/documents/router.py`, `backend/app/schemas/vision.py`,
  `backend/app/services/visual_v2_service.py`, relevant R05 backend tests;
  `frontend/lib/features/documents/data/documents_api.dart`,
  `frontend/lib/features/documents/application/documents_repository.dart`,
  `frontend/lib/features/documents/application/documents_providers.dart`,
  `frontend/lib/features/documents/presentation/documents_page.dart`, at most
  one domain model and one dialog under the documents module, and their tests.
  Documentation is limited to the canonical roadmap/R05 mirrors/D-20, the A3
  acceptance note, one A4 report/index, and this start plus handoff checkpoint.
- Evidence root:
  `C:\ai-lab-core-staging\recovery\R05_A4_OPERATOR_APPROVAL_20260912T121418Z`
  (`LOCAL_ONLY`). Initial ACL localization failed before any evidence write;
  the ACL was then verified as non-inherited FullControl only for the current
  user, SYSTEM and local Administrators.
- Tests: `NOT_RUN` at this start checkpoint. Application server, browser,
  worker CLI, Supervisor, Temporary Chat, model, Qdrant, Gmail, n8n and real
  export were not started.
- One next safe step: implement the bounded preview endpoint and operator
  dialog, then run focused backend/API/widget regressions in isolated inputs.
- STOP: no lifespan, real BackgroundTasks target, external transport, Web
  runtime/build, APK/Android, migration, R06, deployment or production write.

Previous checkpoint:
`docs/recovery/checkpoints/20260912T080832Z-R05-A3-BROWSER-FILEPAYLOAD.md`.
