# R04-20260915T051903Z-D21-P1-START

## Owner decision and scope

The owner accepted the 33-row D-21 map and plan published at
`b6cb4c38d8c739ceda87feb2e0c18cf98117ed02` as
`SINGLE_ROOT_STARTUP_MAP_ACCEPTED_FOR_P1 / NO_OPERATIONAL_APPROVAL` and
authorized only `D21-P1 SOURCE / OFFLINE TEST` work:

- implement the missing tracked `operations/runtime/start-host-services.ps1`;
- add bounded native-command execution and a safe existing-resource interface
  to `operations/windows/start-compose-after-docker.ps1`;
- define a minimal fail-closed startup-manifest contract and an
  `EXAMPLE_ONLY / NOT_APPROVED` example;
- test the real planning and decision logic with complete synthetic adapters,
  including a bounded harmless PowerShell child-process test;
- publish source, tests and sanitized evidence only on the recovery branch.

This does not authorize installing or invoking the launcher under
`C:\ai-lab-core`, changing the existing Startup wrapper or tasks, calling
Docker/HTTP/Task Scheduler product boundaries, starting services, cutover,
relocation, cleanup, deployment or P2-P5.

## Verified start state

- recovery worktree/branch:
  `C:\ai-lab-core-recovery` /
  `recovery/next-stabil-repair-completion`;
- local/tracking/remote parent:
  `b6cb4c38d8c739ceda87feb2e0c18cf98117ed02`;
- `origin/main`:
  `483f9bf8b1a591ded8a42df5da87663c664ed5d4`;
- pinned rescue:
  `5cd8f86e63e1ab829692ca2601096fd0c0d9d53a`;
- recovery was clean and staged `0`;
- original worktree preservation: `203/203` present and SHA-256 matching,
  mismatch `0`, staged `0`;
- after excluding the probe process, no host process command line referenced
  `C:\ai-lab-core-recovery`;
- no A4 WIP was present or reapplied.

The D-21 live inventory remains historical input. P1 performs no Docker,
service HTTP, task execution or runtime health observation.

## Selected implementation boundary

The existing R04-A1 component identity vocabulary remains authoritative for
component kinds. P1 adds only an installation/startup contract around an
approved set, with explicit root, paths/hashes, existing-resource identities,
host-process definitions, policy, timeouts and client-opening policy. The
ordinary entrypoint must reject missing, malformed, `NOT_APPROVED`, recovery
or staging manifests before an operational adapter is called.

Production effects and application tests at this checkpoint are `0 / NOT_RUN`.
One next safe step is implementation of the P1 source and focused offline
tests on recovery only.

## In-session progress and formal source-edit blocker

The recovery-only implementation now contains the manifest validator, bounded
PowerShell 5.1 native runner, synthetic-adapter startup decision chain,
existing-container identity gate, candidate host entrypoint, example contract,
README and focused test. The current focused run completed with
`D21_P1_TEST_PASS assertions=40`; no Docker, service HTTP, Task Scheduler or
product process boundary was called.

The tool's safety reviewer rejected two still-required source edits because
they change future execution semantics, even though the installed
`C:\ai-lab-core` copy would remain untouched:

1. disabling/removing the legacy direct `docker compose up -d` default from
   the recovery version of `start-compose-after-docker.ps1`;
2. wiring the real, manifest-bound adapters into ordinary canonical launcher
   execution after all fail-closed checks.

The owner subsequently and explicitly approved both recovery-source changes:
remove/disable the direct `docker compose up -d` default without a fallback,
and wire real manifest-bound adapters behind canonical-root and approved-set
validation. The approval remains `SOURCE / OFFLINE TEST ONLY`; it does not
authorize copying or executing the files under `C:\ai-lab-core`, installing a
task/shortcut, starting services, changing runtime configuration or approving
a production manifest. Final readiness still depends on passing the complete
offline regression on the resulting bytes.

## Completion pointer

The two approved edits were implemented and the resulting source was committed
as `1bfb377a224499eda2366a7cc52814e55ab267fa`. The final PowerShell 5.1 focused
run passed 53 assertions with exit 0. This start note is superseded for current
execution status by
`docs/recovery/checkpoints/20260915T155243Z-R04-D21-P1-HANDOFF.md`; it remains a
historical record of the initial authorization and formal-review boundary.
