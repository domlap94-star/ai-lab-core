# R05-20260914T184044Z-A4-BACKEND-TESTS

## Scope and owner decision

The owner explicitly approved leaving the existing Supervisor
`INTENTIONALLY_STOPPED` and running one isolated backend campaign for R05-A4.
This did not authorize a Supervisor start, producer or queue changes, product
lifespan, external services, models, Temporary Chat, export, deployment or R06.
R05 and R04 remain `IN_PROGRESS`; R03 remains
`WAITING_APPROVAL / WAITING_ESCROW_DECISION`.

Start local/tracking HEAD was
`99adfb86edc79e052b8159c5805e75deba5b6d42`. The four protected WIP files and
patch SHA-256
`81E311E7C129FC7ACC8829BC8B2E3FB31CDFEA6F800D56EE46E16E9E58A17914`
matched the preceding checkpoint. The original worktree remained at
`72950657ac79b50d0afe72753632ba4cde810b95` with 6 modified plus 197 untracked
entries and staged 0.

## Preflight and isolation

Docker Engine `29.8.0` answered through `desktop-linux`. The pinned R02 image
was present as
`sha256:4b12cf0e2501981eff4d7ce6cfd5eb55fcc83ae41bf5561b565e7aa8aed37651`.
The active backend retained full ID
`9d9b46c530412e48562b5427a0586b08c1e919ff293ec2cbd1489faffc98615a`,
image ID
`sha256:6342b36fa2cdd2501ea4e0e9fada9a9ffaa4894f0c512f19f822f009e8d63702`
and `/app=C:/ai-lab-core/build/deploy-main-483f9bf8/backend`; no recovery or A4
WIP mount was found. The previous exact-name A4 container was absent.

Before the test, Windows reported `25.802 GiB` physical total, `7.838 GiB`
available and commit `33.057 / 70.802 GiB`, leaving `37.745 GiB`. The matching
docker-desktop pool reported `14.010 GiB` available and unused `8 GiB` swap.
The single test container was:

- name `next-stabil-r05-a4-20260914t183454z-backend-final`;
- full ID `9fbb55aab1aa51375e57a62daa1b255af6907bde32368e0d35abf52f3c1a446d`;
- owner/scope/run `codex / R05-A4 / 20260914T183454Z`;
- `network=none`, no ports, not privileged, no host PID or Docker socket;
- read-only root and source, tmpfs `/tmp`, with only read-only recovery source
  and the protected evidence directory mounted.

No product server or lifespan ran. The test used synthetic Settings and
SQLite/tmpfs. External connections were impossible through `network=none`.

## Executed tests

The exact preimage was an archive from the committed router blob at
`f3cbe58bed0de34cfd39a57d481abb2c61c2f4ac`.

| Check | Command inside the isolated container | Result |
|---|---|---|
| CORS fail-before | `python -m pytest -q -p no:cacheprovider test/test_r05_visual_export_api.py::test_r05_a4_preview_exposes_exact_headers_only_to_allowed_origin` against the preimage copy | expected FAIL, exit `1`: `1 failed`; missing `access-control-expose-headers` |
| Final focused API | `python -m pytest -q -p no:cacheprovider test/test_r05_visual_export_api.py` against WIP | PASS, exit `0`: `3 passed` |
| Python compile | `python -m compileall -q app/api/documents/router.py test/test_r05_visual_export_api.py` | PASS, exit `0` |

Runtime was Python `3.12.13` and pytest `8.3.5`. The ASGI test used the real
router and CORS middleware. It checked the allowed origin, same-origin
compatibility, no matching allow-origin for a foreign origin, exact exposed
headers and bytes, 401/403/409 negative responses, no new approval, no
background call and no fake Supervisor job.

The earlier Flutter evidence remains bound to unchanged frontend file hashes:
fail-before `6 passed / 4 failed`, focused `11 passed`, Documents/Auth
`28 passed` and analyze PASS. These were not rerun in this continuation.

## Evidence and effects

Sanitized local evidence is under
`C:\ai-lab-core-staging\recovery\R05_A4_PREVIEW_DECISION_20260912T140056Z\isolated-backend-20260914T183454Z`.
The campaign summary is 3064 bytes with SHA-256
`8B149691AC5694632AB61A36C4E9EAADE3A3276EDF2563A7BF675659CEB4DD22`.
Raw logs remain `LOCAL_ONLY` and are indexed in
`docs/recovery/R05_A4_LOCAL_EVIDENCE_MANIFEST.csv`.

Post-test resources still passed the gate: Windows available `7.825 GiB`,
commit headroom `37.497 GiB`, docker-desktop available `13.829 GiB`, swap
unused. Public Gateway, proxied API, Web, backend, n8n and Open WebUI returned
`200`; `/control`, `/control/` and `/control/health` returned `404`.
Supervisor `127.0.0.1:8787` remained unreachable and was not started by this
session.

The test created only its named container, tmpfs files and protected local
logs. After full owner/run/ID verification the container was stopped and
removed; no matching container remains. No producer, queue, spool, schedule,
company data, model, external service or production configuration was changed.

The four tested files were committed as source
`04ab5e58cf86896ffd946cabffde13367d343f53`. This is
`R05 A4 PREVIEW_TRANSPORT_AND_DECISION_FIX_READY_FOR_REVIEW / NOT_DEPLOYED` and
`API_CORS_AND_WIDGET_DECISION_TESTS_PASS`, not owner acceptance. Operator Web
runtime and external end-to-end remain `NOT_VERIFIED`.

## One next safe step

The owner reviews R05-A4 source and evidence. After a separate acceptance, the
next proposed work is the already recorded D-21/R04 single-root and startup
inventory/mapping scope, not R05-A5 or R06.
