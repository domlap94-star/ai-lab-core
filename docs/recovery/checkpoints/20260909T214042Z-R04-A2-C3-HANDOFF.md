# R04-20260909T214042Z-A2-C3-HANDOFF

- UTC: `2026-09-09T21:40:42Z`.
- Branch/worktree: `recovery/next-stabil-repair-completion` /
  `C:\ai-lab-core-recovery`.
- Parent dokumentacji: `6d715ddd0df01b95960951a5a8859003aaabf431`.
- Code under test: `f4ea20c74f92c0423db087ba8d60bb8cc7f2ec99`;
  archiwum `source-f4ea20c.tar` SHA-256
  `B93DF8FE6F2D86EE32394D7DE08EF45DB44E99A97C9A38F6231A44200400F391`.
- Poprzedni checkpoint:
  `docs/recovery/checkpoints/20260909T205617Z-R04-A2-C3-START.md`.

## Wykonany zakres

1. Zewnętrzny profil low-memory i rzeczywisty Gradle guard potwierdziły heap
   `2048m`, metaspace `768m`, code cache `256m`, worker `1`, parallel false,
   offline i Kotlin in-process. Patch apply/reverse = PASS.
   Helper pomiarowy zachowuje domyślne dane C2; parser PowerShell 5.1, odczyt
   C3 oraz odmowy złego pełnego ID/owner/run = PASS.
2. Wykonano dokładnie jeden `assembleDebug`; exit `0`. APK ma `166479535 B`,
   SHA-256 `FAF545C0F6E6DD55DC48898A10714AA099FF3EBE78B648EADAC2B036C7AE325E`,
   package `pl.ailab.app.r04test`, lokalny debug signer i testowy endpoint.
3. Podczas builda minima: Windows `5.602 GiB`, commit `36.065 GiB`, właściwa
   pula Docker/WSL `14.135 GiB`, swap bez wzrostu — bramki PASS.
4. Po kontroli exact-ID/DB/fixture wznowiono zachowany stos A2 i uruchomiono
   istniejący `Pixel_8`. Rzeczywisty serial `emulator-5554` potwierdzono.
   Przed ukończeniem bootu Windows available spadł do `3.900 GiB`; monitor
   zwrócił `RAM_GATE_BLOCKED`. Dokładny emulator zatrzymano, bez retry,
   instalacji APK i bez Android UI-01–UI-06.
5. Web UI-03 nie ponawiano: widoczna treść nadal
   `WAITING_OWNER_VISUAL_EVIDENCE`. `R04-A2-UI06` nadal
   `KNOWN_DEFECT_OPEN_R16`; D-15/D-16 i modele `NOT_RUN`.

## Stan zasobów i skutki

- Postgres/backend/transport A2: `exited`, zachowane.
- Telemetria C3: usunięta dopiero po pełnym ID/owner/run check.
- Emulator/adb oraz własne procesy Java/Dart/Flutter: zatrzymane/zakończone.
- Listenery `127.0.0.1:18004/18005`: `0`.
- Testowe DB, storage, cache i jeden APK: zachowane `LOCAL_ONLY`.
- Produkcja, main, rescue, oryginalny dirty worktree, modele, kolejki, backup,
  escrow i zasoby R03: bez działań C3.

## Status i następny krok

- R04-A2: `APP_SMOKE_PARTIAL / TEST_ONLY`.
- Android: build PASS; runtime `RAM_GATE_BLOCKED`; UI-01–UI-06 `NOT_RUN`.
- R04: `IN_PROGRESS`.
- R03: `WAITING_APPROVAL / WAITING_ESCROW_DECISION`.

Jeden następny bezpieczny krok: właściciel ocenia częściowy wynik C3;
ewentualne osobne okno AVD-only z zachowanym APK wymaga nowej zgody i startu
z co najmniej `8.5 GiB` Windows available przy zachowaniu dotychczasowych
bramek. STOP przed R16, dalszym R04, produkcją, modelami i escrow.
