# R04 / D-21 / P4-B — host-services cause diagnosis

UTC: `2026-09-23T09:50:13.5151643Z`

Status: `P4B_HOST_SERVICE_CAUSE_DIAG_PARTIAL / PRIVATE_SUPERVISOR_LISTENER_EMPTY_NORMALIZATION_REPRODUCED / PUBLIC_PROCESS_TOKEN_EVIDENCE_UNRESOLVED / NO_RETRY / STAGE_B_BLOCKED_NOT_AUTHORIZED`

## Preserved entry state

- Entry/evidence HEAD: `32abe2e374a0984291cf3e1a48ed0dee90b6117e`.
- Exact observer: `7940` B,
  `4DCFE146ED51A8F68FECEB8DCB10FD1A873F4D88CC0892EDC372574B8B7890F9`.
- Accepted launcher/runtime:
  `B4143A6934E6723E3293A6D5A4806A4F01D34CB53037C6417B16830BD4EC7892` /
  `959768E297BCB93FF1AF3D7EE5A174313F9DC053707EDE8C45D3A84D024B0732`.
- Preserved `o1\host.json`: `2561` B,
  `DA2274B770CBD5351BAB0B4A51B884739DD4CA49CA3D28FC32BA60533FE23CB8`.
  The .NET SHA-256 was rechecked without changing the file.
- Its historical results remain Public `CONFLICT / PORT_OWNERSHIP_CONFLICT`,
  Private and Supervisor `UNKNOWN / OBSERVATION_UNKNOWN`, with starts/task
  writes/Docker/HTTP reads `0`.
- D-23 review remains `2/2`; Host22 and NUP-01/02/03 acceptances are preserved.
  No K2/K3 review was opened.

## Collector and offline gate

The collector is LOCAL_ONLY under
`C:\ai-lab-core-recovery\build\P4B-HS-REVIEW-01\d1`. It has a closed list of
`TASK`, `NODE_PROCESSES`, `LISTENER` and `PID_PROCESS` reads, a shared `120 s`
campaign deadline, a `20 s` maximum per leaf, exact Windows PowerShell 5.1,
and records each safe leaf before aggregation.

- collector: `45774` B,
  `CB3CF6F1C084A250769FEB4B252F6A850D183D4D84C32EE1CBAE3FF7C5300E8D`;
- offline harness: `5276` B,
  `AE880F521EADED3E4FCCD3E5A52AC17020B5DF310FC3AA8C3968214E7406C818`;
- final offline result: `5` cases / `20` assertions PASS, production boundaries,
  starts and task writes `0`; result `319` B,
  `01BBA9A104877BB7ED1E2EFDA496483C6974E28CDA9873CC86A16AD472337EAC`.

The five cases exercised actual local write/read parsing for a valid result,
empty success, exact `ObjectNotFound`, access denied and incomplete data. The
first harness invocation stopped before creating output because dot-sourcing
the library replaced the test's output parameter. The minimal local binding
fix was applied and parsed before the final PASS; this was a harness issue, not
a product test or host result.

## One live read-only campaign

- engine:
  `C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe`,
  `5.1.26100.8894`, Desktop;
- parent PID/start: `41000`, `2026-09-23T09:44:36.4359712Z`;
- observation window:
  `2026-09-23T09:44:36.8356178Z`–`2026-09-23T09:44:49.2008688Z`;
- elapsed: `12364 ms`, within the shared deadline;
- leaves: `8`, including one exact non-node port-owner PID read;
- every child accounted, timeouts `0`, stdout/stderr complete;
- starts, task writes, Docker reads and HTTP reads: `0`;
- live aggregate: `6600` B,
  `2B829198A0811397D79B6E09DB391E29AEE6A5E79BDBEB517A9FBBE0830D21B3`.

No leaf was repeated. The exact task reads and the node/PID CIM reads reached
their system cmdlets, but the subsequent safe projection failed because
launcher functions imported inside a helper were not visible in the caller's
scope. The persisted errors are local `CommandNotFoundException` records; no
safe task/process object remained available to reformat. Per the owner rule,
there was no second host read.

## Consolidated three-role diagnosis

| Role | Previous result | Documented leaf / first predicate | Expected -> observed | Classification | Minimal next step |
|---|---|---|---|---|---|
| Public Gateway | `CONFLICT / PORT_OWNERSHIP_CONFLICT` | `ObserveHostService.foreignListener` is the first predicate selected by the accepted adapter when this exact detail is returned | one exact process PID and listener `127.0.0.1:8789` owned by it -> current listener is `127.0.0.1:8789`, PID `41784`; current exact-process/token comparison was not persisted | `POTWIERDZONE_W_KODZIE`; deeper owner cause `UNKNOWN` | One corrected process-only projection for PID `41784`: CreationDate, executable and safe token comparison; do not repeat task/listener |
| Private Gateway | `UNKNOWN / OBSERVATION_UNKNOWN` | catch after `GET_NET_TCP_LISTENER`; `Test-ExpectedEmptyResultError` returned false | exact no-match for `8788` should normalize to an empty listener set -> category `ObjectNotFound`, FQID `CmdletizationQuery_NotFound,Get-NetTCPConnection`, but a nonempty TargetObject (safe hash `3D345D...C3994`) was not equal to scalar port `8788` | `ODTWORZONE` | Future source/offline decision: successful bounded Listen snapshot plus local exact-port filter; errors remain UNKNOWN |
| Supervisor | `UNKNOWN / OBSERVATION_UNKNOWN` | same listener catch and predicate | exact no-match for `8787` should normalize to empty -> same category/FQID and same non-port TargetObject hash, not scalar `8787` | `ODTWORZONE` | Same single listener-normalization diff; preserve `INTENTIONALLY_STOPPED` |

The listener leaf files are:

- `8789`: `851` B,
  `E79C28C2E5289AC1AE625A025E4CE0019F08D11D3317ED3D1A36432F467D7B12`;
- `8788`: `1172` B,
  `39D74E84054FF944DD6C20E8FDB08510B9A844E218C016FDB2017D27C671A30D`;
- `8787`: `1172` B,
  `6C626935141D728BBB04DF067ED901B85AEC90F0B7FAB74AB861180A2379F682`.

Private/Supervisor are not relabelled `ABSENT`: the failed safe process
projection means process presence is not proved either way. For Public, the
accepted code resolves `actualCodePath` but then compares the raw code token
to the manifest token. Historical evidence supports an absolute-vs-relative
hypothesis, but the owner explicitly disallowed treating the reused PID alone
as proof of the same current process. No process comparator change is proposed
as proved until the one missing current projection exists.

## Boundaries and one decision

The local diagnostic root contains `34` files / `79400` B and remains ignored,
LOCAL_ONLY. It is not an install package and is not committed. Installed run01,
Host disabled/no-trigger, warm runs `0/2`, the six containers, backend flags,
Supervisor policy, tasks, ports, data, junction and backups were not changed.
There was no VerifyOnly, Docker, HTTP, SQL, UAC, InstallAndWarm, Host/service
start, rollback, P5/R06 or D-22 work.

One recommended owner decision: allow or decline exactly one corrected
process-only read of PID `41784`, with CreationDate and safe executable/token
projection, without repeating tasks/listeners or any other preflight. If it is
allowed, its result can be combined with the already reproduced listener bug
into one source/offline decision. Stage B remains blocked and unauthorized.

> **ANTI_EXCESSIVE_WORK — OBOWIĄZKOWY ODCZYT PRZEZ CHATGPT**
>
> Before assessment, read roadmap §0, `ANTI_EXCESSIVE_WORK`, the active R04 card
> and this checkpoint at the published SHA. Preserve all prior acceptances.
> D-23 remains `2/2`; do not open K2/K3 or a third general review.
>
> K0/K1: `P4B-PREFLIGHT-HOST-RUNNER` — Private/Supervisor exact no-listener
> errors are not normalized because `TargetObject` is not the scalar port;
> Public's first rejecting predicate is known, but current PID `41784` process
> creation/token evidence was not persisted after a local collector projection
> failure. Effect: host-services identity/state cannot pass and Stage B remains
> unauthorized. Next action: one owner decision on the exact process-only
> projection above; no host operation is authorized by this checkpoint.
