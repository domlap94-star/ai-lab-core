# R04-20260910T175215Z-A2-UI06-A-REPEAT

- UTC: `2026-09-10T17:52:15Z`.
- Branch/worktree: `recovery/next-stabil-repair-completion` /
  `C:\ai-lab-core-recovery`.
- Parent documentation HEAD:
  `25c30e7dd9e3d451772eee812bdef190665e1b8d`.
- Accepted frontend source/test:
  `48fbecae0a76edb25f60e9dd314bb8d65bfbae4b`.
- Saved synthetic backend source:
  `f4ea20c74f92c0423db087ba8d60bb8cc7f2ec99`.
- Previous checkpoint:
  `docs/recovery/checkpoints/20260910T152711Z-R04-A2-UI06-OWNER-HANDOFF.md`.
- Authorization used: the owner's current instruction `powtorz A`, interpreted
  only as one additional owner-operated A attempt on the preserved test set.

## Result

The repeat did not satisfy the strict first-document-list-read gate.

1. The owner was authenticated with the test backend running.
2. The exact backend was stopped at `2026-09-10T17:43:12.3516056Z` and the
   owner performed a full refresh in the same InPrivate session.
3. The app stopped earlier at session restoration and displayed
   “Nie udało się sprawdzić sesji. Sprawdź połączenie i spróbuj ponownie.”
   It did not reach `DocumentsController`.
4. The same full backend ID was restarted at
   `2026-09-10T17:47:10.0709952Z`; health and source identity passed.
5. The owner refreshed and confirmed Dashboard without clicking Documents.
   The backend log nevertheless contained successful `GET /api/v1/auth/me`
   followed by
   `GET /api/v1/documents?link_state=ALL&skip=0&limit=6`.
6. Because the session-level no-prefetch condition was already false, no
   second outage, Documents click, retry, or retry-to-success was performed.

Source review distinguishes this Dashboard request from the page controller:
the `/documents` route constructs `DocumentsController`, whose page size is
50; the observed request used limit 6. The approved manual procedure still
requires no prior list retrieval in the session, so that distinction does not
permit a PASS.

Verdict: `A=NOT_VERIFIED / AUTH_GATE_THEN_DASHBOARD_PREFETCH`. This is a
procedure/evidence limitation, not `WEB_REGRESSION_FAIL`. Historical
`B=PASS / OWNER_OPERATED_RUSTDESK` remains unchanged and was not rerun.

## Evidence and resources

- Owner-provided screenshot copied to protected evidence as
  `scenario-a-auth-gate.jpg`: `198361 B`, SHA-256
  `FFA564DDD065F7F6738829DAA5ECA41EAAE8DBDDA51CB4F24135E556C8D69ED6`.
- Local evidence root:
  `C:\ai-lab-core-staging\recovery\R04_A2_REAL_APP_20260909T131617Z\owner-ui06-a-repeat-20260910T173258Z`.
- Resource monitor: `68/68 PASS`; minima Windows available `4.539 GiB`,
  commit reserve `33.645 GiB`, Docker/WSL pool available `13.808 GiB`;
  maximum swap use `1.9 MiB`.
- One backend stop/start occurred for the auth-gate attempt. No second outage
  occurred after Dashboard prefetch was observed.
- Final bounded DB gate required database `ai_lab_r04_a2_20260909`, revision
  `followup_assistant_chat_history_20260829`, 2 clients and 1 document. HTTP
  logs contained no business POST/PUT/PATCH/DELETE. Authentication/session
  checks may have technical audit effects.
- Final states: PostgreSQL/backend/transport `exited`; Web stopped; telemetry
  `b40aae3a8a93...` removed after full ID/owner/run verification; listeners on
  `18004/18005`: 0. Synthetic DB/volume/storage/cache/APK/evidence preserved.

Three safety checks stopped before their intended mutation and were corrected
without changing the test subject: Docker represented empty telemetry ports as
`{}` rather than `null`; the first frontend path check used `presentation`
instead of the actual `application` path and therefore started no Web; the
first stop guard used an incorrectly expanded backend ID and therefore did not
stop the backend. Each subsequent action used the manifest/full ID and passed.

Application tests: `NOT_RUN`; product source/tests were unchanged. Production,
R03 resources, models, KB, Vision, Temporary Chat, Qdrant, Gmail, n8n, queues,
migrations, backup, escrow, main and rescue were not used or changed.

## Handoff

Status: `R04-A2 UI06 SOURCE_ACCEPTED / WEB_AB_PARTIAL / TEST_ONLY`;
`A=NOT_VERIFIED`, `B=PASS`; `UI_OBSERVER=OWNER_OPERATED_RUSTDESK`;
`NOT_DEPLOYED`. R04 remains `IN_PROGRESS`; R03 remains
`WAITING_APPROVAL / WAITING_ESCROW_DECISION`; Android remains
`DEFERRED_BY_OWNER / NOT_TESTED`; D-15/D-16 AI acceptance remains `NOT_RUN`.

One next safe step: owner and ChatGPT review the preserved B result and the
twice-inconclusive A path. Any selective test that blocks only the documents
endpoint needs a separate, exact approval; do not start it automatically or
begin `ClientsController`, Android, R16, R05–R24, deployment, or another fix.
