# R04-20260909T092544Z-A1-SOURCE

- UTC: `2026-09-09T09:25:44Z`
- Pakiet/podetap: `R04 / A1 VERSION_IDENTITY_SOURCE`
- Status: `SOURCE_READY_FOR_REVIEW / NOT_DEPLOYED`; cały R04: `IN_PROGRESS`
- Branch/worktree: `recovery/next-stabil-repair-completion` / `C:\ai-lab-core-recovery`
- Start checkpoint commit: `2c8bb819bfb768a8942416059ad0aea2b8083cac`
- Source/test commit: `ec102a8dacac529c3ac5f6715c08e4dd3ac35020`
- Source baseline: `origin/main@483f9bf8b1a591ded8a42df5da87663c664ed5d4`
- Rescue: `5cd8f86e63e1ab829692ca2601096fd0c0d9d53a`, nieadoptowane

## Zakończone

- Zachowano wszystkie legacy top-level fields `/version`; dodano wyłącznie
  allowlistowaną `component_identity` i opcjonalne deklarowane identity fields.
- Dodano realny walidator dziesięciu komponentów. Różne numery składników są
  prawidłowe; `UNKNOWN`/brak/błędny hash nie daje `VERIFIED`, a prawidłowy inny
  artefakt daje `MISMATCH` bez ujawnienia jego wartości.
- Stable/update manifest, min/latest policy i produkcyjny parser nie zostały
  zmienione. Istniejący klient Flutter odczytuje stable fixture oraz ignoruje
  addytywne metadata bez zmiany decyzji aktualizacyjnej.
- Publiczny endpoint nie uruchamia lifespan, migracji, dispatcherów ani usług.
  Nie publikuje ścieżek, command lines, host inventory ani sekretów.

## Dowody bieżącego wykonania

- Fail-before: pytest collection `ModuleNotFoundError` dla brakującego realnego
  `version_identity_service`, exit `2`.
- Focused pass-after: `10/10`, exit `0`.
- Backend combined: `21/21`, exit `0`.
- Public gateway CORS: `PASS`, exit `0`.
- Python compile: `4/4`, exit `0`.
- Flutter update/stable parser: `10/10`, exit `0`.
- Flutter analyze: `No issues found`, exit `0`.
- `git diff --check`: `PASS`.
- Obraz testowy: `sha256:4b12cf0e2501981eff4d7ce6cfd5eb55fcc83ae41bf5561b565e7aa8aed37651`;
  source read-only, network none, wyłącznie syntetyczne env/tmpfs.
- Pełny dowód i komendy: `docs/recovery/R04_A1_VERSION_IDENTITY_EVIDENCE.md`.

## Runtime i skutki

- Read-only observed backend: `9d9b46c53041`, image ID `sha256:6342b36f...`,
  `/app=C:/ai-lab-core/build/deploy-main-483f9bf8/backend`.
- Publiczne live `/version`: legacy fields, backend `1.0.0`, API `1`,
  environment `development`, `debug=true`, minimum/latest `1.0.0`, brak
  `component_identity`. Wniosek: candidate A1 jest `NOT_DEPLOYED`.
- DB nie odczytywano ani nie zmieniano. Runtime/config/build/release/model/queue:
  brak operacji. Testowe kontenery po `--rm`: `0`.
- Flutter odświeżył zależności istniejącego locka/cache; sześć dotkniętych
  generated registrants miało bloby identyczne z HEAD i nie weszło do diffu.
- Oryginalny dirty worktree pozostaje 6 modified + 197 untracked, staged 0,
  łącznie 203 pozycje; main/rescue niezmienione przez tę sesję.

## Zachowane bramki

- R03-A4 pozostaje zaakceptowane na `92cc3aa38f0a81ff40e3798981970bd2e45b6206`
  dla manifestu `8F20A784...`; cały R03 pozostaje
  `WAITING_APPROVAL / WAITING_ESCROW_DECISION`.
- R04-A1 nie jest samodzielnie ACCEPTED. Pozostałe podetapy R04 i R05–R24 nie
  zostały uruchomione. Nie wykonano escrow, cleanupu ani adopcji rescue.

Następna bezpieczna czynność: odbiór R04-A1 przez właściciela. Dopiero osobna
zgoda może rozpocząć następny podetap istniejącej karty R04.

Poprzedni checkpoint: `docs/recovery/checkpoints/20260909T085533Z-R04-A1-START.md`.
