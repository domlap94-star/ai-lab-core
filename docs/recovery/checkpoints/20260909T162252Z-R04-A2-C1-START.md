# R04-20260909T162252Z-A2-C1-START

- UTC rozpoczęcia: `2026-09-09T16:22:52Z`.
- Pakiet/podetap: `R04 / A2 C1 COMPLETION_EVIDENCE`.
- Stan: `IN_PROGRESS / TEST_ONLY`; cały R04 pozostaje `IN_PROGRESS`.
- Branch/worktree: `recovery/next-stabil-repair-completion` /
  `C:\ai-lab-core-recovery`.
- Clean local/remote start: `5809350704916bf03aec9b8d39575fd0247fb7b8`.
- Code under test: `f4ea20c74f92c0423db087ba8d60bb8cc7f2ec99`;
  main `483f9bf8b1a591ded8a42df5da87663c664ed5d4`, rescue
  `5cd8f86e63e1ab829692ca2601096fd0c0d9d53a` bez adopcji.
- Poprzedni checkpoint:
  `docs/recovery/checkpoints/20260909T150156Z-R04-A2-HANDOFF.md`.

Właściciel przyjął częściowy dowód Web opublikowany na `5809350704916bf...`
i zezwolił wyłącznie na C1: korektę recipe/RESUME, bieżące read-only pomiary
zasobów, wizualne domknięcie dotychczasowego Web UI-03 oraz Android UI-01–06
tylko po pełnej bramce zasobów. A1 pozostaje `ACCEPTED / NOT_DEPLOYED`;
R03 pozostaje `WAITING_APPROVAL / WAITING_ESCROW_DECISION`. D-15/D-16 nie są
testowane przez AI w C1.

Preflight potwierdził czysty Git, brak locka/konkurencyjnego autora, pełne ID
trzech zatrzymanych kontenerów zgodne z chronionym `resource-manifest.json`,
właściwe obrazy, owner/run labels, mounty, sieci i zachowany wolumen. Archiwum
source ma SHA-256 `B93DF8FE6F2D86EE32394D7DE08EF45DB44E99A97C9A38F6231A44200400F391`;
blob `backend/app/main.py` i Web `frontend/lib/main.dart` nadal odpowiadają
`f4ea20c...`. Poświadczenia syntetyczne istnieją w chronionym pliku LOCAL_ONLY;
wartości nie były wypisywane. Nowe dowody: chroniony katalog
`C:\ai-lab-core-staging\recovery\R04_A2_REAL_APP_20260909T131617Z\continuation-20260909T162252Z`;
jego ACL jest dokładnie zgodny z ACL rootu A2.

Fail-before na zachowanej rzeczywistej odpowiedzi `/version`: stary recipe
odczytuje `component_identity.source_revision=MISSING` i zatrzymuje prawidłowy
zestaw; rzeczywista wartość jest pod `component_identity.backend.source_revision`
i równa code under test. To `RECIPE_FIX`, nie wada endpointu.

Pomiar `2026-09-09T16:21:49Z`: Windows physical total/available
`25.802/8.118 GiB`; commit used/limit/reserve `31.629/70.802/39.174 GiB`.
Aktywne pagefile: C: `13312 MiB` przydzielone, `522 MiB` użyte, peak `2660`;
D: `32768 MiB` przydzielone, `677 MiB` użyte, peak `3485`, konfiguracja
`32768–65536 MiB`. D: to osobny dysk NVMe 0, `854.43 GiB` wolne, bieżące I/O
`0 MiB/s`; C: to NVMe 1. SMART poza `HealthStatus=Healthy` jest `UNKNOWN`.
WSL CLI zwrócił `E_ACCESSDENIED` i nie był eskalowany. Alternatywny bounded
read-only `/proc` z już działającego kontenera w tej samej puli Docker podał
`MemTotal=17.563 GiB`, `MemAvailable=14.143 GiB`, swap `8 GiB`, użycie około
`2 MiB`; Docker `stats` było jednorazowe. Windows pagefile nie został doliczony
do physical available. Krótki Web może przejść; Android wymaga ponownego gate
po zakończeniu Web i po buildzie.

Allowlista Git C1: `operations/recovery/r04-a2/README.md`, ewentualny mały
build-only overlay/helper wyłącznie pod tym katalogiem, bieżący raport/manifest
A2, checkpointy C1, §0/odnośnik R16 roadmapy oraz tylko konieczne lustra.
`backend/app`, `frontend/lib`, masterplan i followup są wykluczone.

Następny bezpieczny krok: poprawić ścieżkę guardu i dodać jawny RESUME bez
root/migration/seed, następnie przeprowadzić pozytywne/negatywne testy recipe.
Przed startem Android obowiązuje nowy pomiar; bez wymaganej rezerwy lub
obserwowalności zakończyć jako właściwa blokada. Bez modeli, produkcji, escrow,
naprawy UI-06, dalszego R04 i R05–R24.
