# R03 A1 — bramka przed capture

- Checkpoint ID: `R03-20260908T135315Z-A1-CAPTURE-GATE`
- UTC: `2026-09-08T13:53:15Z`
- Repo / branch / worktree: `domlap94-star/ai-lab-core` / `recovery/next-stabil-repair-completion` / `C:\ai-lab-core-recovery`
- Tool source: `c6faca0dba5944a9900cef3cd20f24d35c3060f7`
- Source baseline: `origin/main@483f9bf8b1a591ded8a42df5da87663c664ed5d4`
- Rescue: `5cd8f86e63e1ab829692ca2601096fd0c0d9d53a`, nieadoptowane
- Pakiet / podetap: `R03 / A1 CAPTURE GATE`
- Status: `AUTHORIZED / NOT_STARTED`; cały R03 pozostaje `WAITING_APPROVAL`

## Dokładnie zatwierdzony cel

- Katalog: `E:\ai-lab-backup\20260908T135012Z`.
- Cel nie istniał przy wyborze i ponownej kontroli.
- Writer ma utworzyć katalog bez `-Force`, ustanowić restrykcyjny ACL przed
  artefaktami i atomowo zapisać manifest dopiero po ich kontroli.
- Zakres: DB, cztery katalogi document storage, release stable, dwie kolekcje
  Qdrant, workflow i zaszyfrowane credentials n8n, niesekretna konfiguracja
  oraz zanonimizowany runtime inventory.
- Tryb: `RecoveryPointV2` + `CaptureOnly`; firmowy restore/drill nie może zostać
  wywołany i ma pozostać `NOT_RUN_WAITING_APPROVAL`.

## Bramka narzędzi i izolacji

- Commit `228b4898010d4bb805ec593316e338326301dcdf` dodał wielokolekcyjny writer,
  czytnik V1/V2 i jawnie izolowany proof; commit
  `c6faca0dba5944a9900cef3cd20f24d35c3060f7` usunął odczyt `.env` oraz pełne
  `docker inspect` z capture inventory.
- R03 tool contract: `38/38 PASS`; recovery PowerShell: `15/15 PASS`.
- Istniejące Qdrant validator, backup storage i scheduler: `PASS`.
- Pełny syntetyczny proof: `PASS`; sieć internal, porty hosta `0`, osobne
  Postgres/Qdrant, zasoby po cleanup containers/volumes/networks `0/0/0`.
- Sanitizowany log proof jest `LOCAL_ONLY`, SHA-256
  `EBBEFECB5276B670A6E885E592236FDEBC0247FCEC6E0696AAE86E0B72812BBD`.

## Bieżący preflight

- DB head: `followup_assistant_chat_history_20260829`.
- Aktywne Assistant/Preparation/Analysis/Backup/Restore: `0/0/0/0/0`;
  Preparation queued: `15`, bez zgody na drain.
- Trzy zadania backupu: `Ready`; trzy harmonogramy DB: `synced`; najbliższe
  wykonanie `2026-09-08T23:00:00Z`, więc brak kolizji z bieżącym oknem.
- Źródła jawne: około `8.05 GiB`; obowiązujący gate to większe z oszacowania
  i `40 GiB`, więc `40 GiB`.
- Wolne: `C: 597.14 GiB`, `E: 738.05 GiB`; backing store widziany z wolumenów
  Postgres/Qdrant odpowiednio około `854/917 GiB`.
- Qdrant: `ai_lab_document_chunks` `57` i `ai_lab_knowledge_base_chunks` `157`;
  oba `1024/Cosine`, aliasy `0`. Counts są pomiarem, nie oczekiwaniem writer-a.
- Runtime jest mixed: backend z czystego `main@483f9bf...`, hostowe gatewaye,
  Supervisor i analysis worker z `C:\ai-lab-core`, Vision worker z
  `C:\ChatGPT-Vision-Worker`. Ich bieżące hashe są w runtime inventory.
- Runtime inventory `LOCAL_ONLY`:
  `C:\ai-lab-core-staging\recovery\R03_A1_20260908T115703Z\runtime-inventory-20260908T135012Z.json`,
  SHA-256 `B8A79CCD47858AAB35BA4F7F09A36F9B317E453507E3A4984DC88F11FF8F0817`,
  `contains_secret_values=false`, resident Ollama `0`.
- Próba importu aplikacyjnego settings została odrzucona przed wykonaniem,
  ponieważ mogłaby materializować sekrety. Zamiast niej użyto wyłącznie sześciu
  jawnie nazwanych flag przez `printenv`; wartości sekretów nie odczytano.

## Granice i następny krok

Capture pozostaje jedyną następną dozwoloną operacją. Zwykłe artefakty backupu,
snapshoty Qdrant i zaszyfrowany export n8n są oczekiwanymi skutkami; danych
biznesowych, kolejek, modeli i usług nie wolno zmieniać. Przy kolizji celu,
aktywnym backup/restore, zmianie tool SHA, braku przestrzeni albo błędzie writer-a
należy zatrzymać operację i zachować prawdziwy stan. Po capture wymagane są
walidacja hashy i nowa osobna decyzja właściciela o izolowanym drill. Escrow
pozostaje `WAITING_OWNER_DECISION`.

Poprzedni checkpoint: `R03-20260908T115703Z-A1-START`.
