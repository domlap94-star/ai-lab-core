# R03 A3 — KB source coverage SOURCE_PASS

- Checkpoint ID: `R03-20260908T202602Z-A3-KB-SOURCE-START`
- UTC start: `2026-09-08T20:26:02Z`
- Repo / branch / worktree: `domlap94-star/ai-lab-core` /
  `recovery/next-stabil-repair-completion` / `C:\ai-lab-core-recovery`
- Start local/remote: `afedb4d1025feb8bd287272c36935ce9feb4f877`
- A2: dowód przyjęty przez właściciela z ograniczeniem `0/10`
  źródłowych plików KB
- Aktywny podetap: `R03 / A3 KB_SOURCE_CAPTURE` — `SOURCE_PASS`
- Status całego R03: `WAITING_APPROVAL`

## Preflight i reprodukcja

- Recovery local/remote, `origin/main` i rescue są zgodne z zatwierdzonym
  punktem; recovery był clean, a drugi wykonawca nie został stwierdzony.
- Oryginalny worktree: `203/203`, mismatch `0`, staged `0`.
- Stary manifest ma niezmienny SHA-256
  `1195AE5BE68CF589D9B9231C85B0BE678C87117FD471034417B9DC8C5620FD57`;
  zatrzymane zasoby A2 zachowują te same ID i stan.
- Bounded read-only inventory wykazał `10/10` źródłowych plików KB,
  `10/10` zgodnych rozmiarów i SHA-256, `0` missing/mismatch/outside-root i
  `0` reparse points.
- Fail-before na prawdziwym writerze ze startowego SHA i syntetycznym storage
  zakończył się exit `1` na
  `real_writer_declares_kb_source_domain`. Log LOCAL_ONLY:
  `fail-before-real-writer.log`, SHA-256
  `D3B56B9C8D46011CF0AC58AC315AB333B363A87DA2D360205DE1029E7A7614B4`.
  Pierwsza próba przygotowania samej nakładki miała błąd parsera i została
  zachowana osobno; nie jest dowodem produktu.

## Minimalna poprawka i testy

- Bieżący V2 deklaruje pięć domen storage, w tym `knowledge-base`, oraz
  addytywny `NEXT_STABIL_STORAGE_COVERAGE_V1`.
- Writer przed pierwszym zapisem odpytuje wszystkie trwałe rekordy KB w
  transakcji read-only, sprawdza root, rozmiar i SHA-256, a następnie porównuje
  inventory przed i po archiwizacji.
- Reader nadal odczytuje historyczne V1 i V2. Stary V2 bez nowego kontraktu ma
  `storage_coverage_status=NOT_RECORDED` i nie dowodzi aktualnej kompletności
  pełnego storage.
- Syntetyczny writer/reader/guard contract: `58/58 PASS`.
- Recovery wrapper i niezależny manifest narzędzia: `15/15 PASS`.
- Qdrant validator, backup storage i backup scheduler: `PASS/PASS/PASS`.
- Testy obejmują brak i wyjście poza root, size/hash mismatch przy tym samym
  rozmiarze, pusty potwierdzony korpus, reparse point, zmianę inventory w oknie,
  brak domeny w archiwum, V1/V2 i brak automatycznego restore.

Testy użyły wyłącznie syntetycznych plików i zamockowanych granic
PostgreSQL/Qdrant/n8n. Nie utworzyły zasobów Docker, nie dotknęły produkcyjnej
DB ani kolejek. Jeden sandboxowy przebieg Node zwrócił `spawn EPERM`; poprawny
przebieg poza sandboxem przeszedł i ten błąd jest ograniczeniem środowiska,
nie wynikiem produktu.

## Skutki i następny krok

Do tej chwili nie wykonano nowego backupu ani snapshotu, restore, escrow,
modelu, kolejki, deploymentu lub restartu. Utworzono wyłącznie chroniony root
dowodowy A3 i usunięto własne efemeryczne kopie syntetyczne po zachowaniu
logów.

Następny bezpieczny krok: jawnie zacommitować i opublikować ten SOURCE_PASS
na recovery, odczytać opublikowane bajty, a dopiero potem wykonać ponowny
preflight pojemności/okna i jeden autoryzowany `CaptureOnly`.

Poprzedni checkpoint: `R03-20260908T180734Z-A2-DRILL`.
