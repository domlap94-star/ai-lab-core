# R04 / D-21 / P4-B — Public Gateway PID 41784 process-only

UTC: 2026-09-23 12:04:40Z
Entry HEAD: `fb9356ec11827c3d1f3ea5cf4f842045337cf2cf`
Scope: owner-authorized single process-only read plus offline replay; no product
change and no Stage B.

## Result

The focused PowerShell 5.1 harness passed `10/10` cases and `113` assertions;
all `10/10` children were accounted and production process queries were `0`.
Exactly one live query was then issued:

`Get-CimInstance -ClassName Win32_Process -Filter 'ProcessId = 41784'`

It ran under exact Windows PowerShell 5.1 from
`2026-09-23T11:55:13.0319978Z` to `11:55:14.3740950Z`, child PID `44112`, exit
`0`, no timeout and no retry. It returned one complete process record:

- PID `41784`, `node.exe`;
- executable `C:\Program Files\nodejs\node.exe`;
- valid CreationDate `2026-09-14T17:09:01.3918090Z`;
- CommandLine present but retained only in memory;
- parsed `argv_count=2`, no additional arguments;
- expected relative code token versus observed absolute code token
  `C:\ai-lab-core\operations\gateway\public_web_server.cjs`;
- resolved canonical script path MATCH and remaining arguments MATCH.

The historical listener record observed `127.0.0.1:8789`, PID `41784`, at
`2026-09-23T09:44:45.3839234Z`. CreationDate predates that observation and the
new process query, proving temporal continuity of that process instance. TCP
was intentionally not queried again, so current port ownership was not
re-observed.

The unchanged real adapter was replayed from the saved safe process/listener
projections with every external boundary stubbed. It still returned `CONFLICT /
PORT_OWNERSHIP_CONFLICT`, `match_count=0`. The first rejecting condition is
`RAW_CODE_TOKEN_MISMATCH_AFTER_RESOLVED_PATH_MATCH`: the adapter resolves the
correct script but then compares the raw relative/absolute token literally.

Classification:

`PUBLIC_GATEWAY_REPRESENTATION_DIFFERENCE_PROVEN /
PRIVATE_SUPERVISOR_LISTENER_EMPTY_NORMALIZATION_REPRODUCED /
SOURCE_FIX_NOT_AUTHORIZED / STAGE_B_BLOCKED_NOT_AUTHORIZED`.

## Evidence and effects

Protected LOCAL_ONLY root:
`C:\ai-lab-core-recovery\build\P4B-HS-REVIEW-01\p1-20260923T113704Z`
(`160` files, `167513` B; failed harness attempts were preserved as tool
history).

- final harness: `9580` B,
  `4359D42E0D1D46F76EBF8B7D88C5A6B8243058053D9366E66F86675C86BBDF14`;
- final projection: `21734` B,
  `8E3C2CEF1E81DA67ACF0E4AC179BCBDACEFC0A3E288FFF708987F071F9B3AEE7`;
- offline summary: `5140` B,
  `CBF4F4CFB12E51265E283257D558EE9BF342F0BAF31AA2A2FA0761CA770468BE`;
- live safe result: `1377` B,
  `3FACBFE6C078201860552E1CF1A32D4C8196B594A889587C22C7F1DC56DF0D7E`;
- adapter replay: `1016` B,
  `10FE95CE3E91A4D9FA8A3BAC4CD42E46AFEC7D6807261BB11E621BAB585692A0`;
- combined analysis: `3021` B,
  `88A789F69178B42B86770E13AEE909C2225A7092FDADC897FAD2D680F2B1AD56`.

Live counts: process queries `1`; task/listener/other-process reads `0`; starts,
writes, Docker, HTTP, UAC, installation, warm runs and rollback `0`. All child
processes ended. Installed run01, Host, Private Gateway, Supervisor, containers,
data and D-22 were not changed.

## One next decision

Decide whether to authorize one consolidated SOURCE/OFFLINE change that:

1. compares only the service code token by its canonical resolved path while
   retaining exact executable, token count, remaining arguments and extra-arg
   rejection; and
2. uses one successful bounded listener snapshot followed by local exact-port
   filtering, while timeout/provider/read failures remain `UNKNOWN`.

No live retry, Stage B, UAC, installation, Host/task start or rollback is
authorized by this checkpoint.

## ANTI_EXCESSIVE_WORK — OBOWIĄZKOWY ODCZYT PRZEZ CHATGPT

Przed oceną i kolejnym promptem przeczytaj na pełnym opublikowanym SHA §0,
ANTI_EXCESSIVE_WORK, aktywną kartę R04 i ten checkpoint. Zachowaj odbiory;
D-23 review pozostaje `2/2`. Naprawiać tylko wykazany istotny wpływ, K2/K3 nie
ruszać i nie tworzyć z nich automatycznych zadań. K0/K1: błędne odrzucenie
właściwego Public Gateway przez raw relative/absolute token oraz UNKNOWN dla
pewnego pustego listener snapshotu — dowody powyżej. Następny krok to jedna
decyzja o skonsolidowanym SOURCE/OFFLINE diffie; brak zgody na operacje hosta.
