# R04-20260909T172734Z-A2-C1-HANDOFF

- UTC: `2026-09-09T17:27:34Z`.
- Branch/worktree: `recovery/next-stabil-repair-completion` / `C:\ai-lab-core-recovery`.
- Parent dokumentacji: `5809350704916bf03aec9b8d39575fd0247fb7b8`.
- Code under test: `f4ea20c74f92c0423db087ba8d60bb8cc7f2ec99`; archive SHA-256 `B93DF8FE6F2D86EE32394D7DE08EF45DB44E99A97C9A38F6231A44200400F391`.
- Poprzedni checkpoint: `docs/recovery/checkpoints/20260909T162252Z-R04-A2-C1-START.md`.
- Zakres zgody: wyłącznie R04-A2/C1 — recipe/RESUME, bounded resource reads, Web UI-03 oraz Android po pełnej bramce. Bez zmian produktu, R16 fix, modeli, produkcji, escrow, release/deploy i dalszych pakietów.

## Zakończone

1. Poprawiono tylko recipe: właściwa ścieżka to
   `component_identity.backend.source_revision`; guard zatrzymuje brak,
   `UNKNOWN`, zły format i inny SHA. Fail-before i pass-after wykonano na
   zachowanej rzeczywistej odpowiedzi `/version` oraz syntetycznych
   negatywnych obiektach. Dodatkowo znormalizowano tablicę klientów dla
   PowerShell 7, aby RESUME nie dawał fałszywego braku 2/2 rekordów.
2. Zweryfikowano pełne ID/obrazy/owner/run labels/mounty/sieci trzech
   zachowanych kontenerów. Wznowiono je bez migracji i seeda; potwierdzono
   `current_database()=ai_lab_r04_a2_20260909`, head
   `followup_assistant_chat_history_20260829`, `/version=f4ea20c...`, 2/2
   syntetyczne sprawy i dokument 1/1 o rozmiarze 204 B.
3. Web UI-03 wykonał realny przepływ aplikacji do dialogu dokumentu i akcji
   „Otwórz plik”. Dwa diagnostyczne pobrania mają po 204 B oraz oczekiwany SHA
   `C0EBC642C0AE14C7A3D8D4A4A6B5F9178E0370CDC3241E3155E710D88E7EB0F2`.
   Screenshot dialogu: SHA `874737B27AEF5D90B11D2ECFA952B98643E83FC95F8DB8A759003B477638CB9B`.
4. Treść pobrania nie ma screenshotu: in-app browser odmówił `file://`, a
   local viewer w nieinteraktywnym kontekście nie udostępnił okna. To
   `BLOCKED_TOOLING/PARTIAL`, nie FAIL aplikacji i nie wizualny PASS.
5. Wszystkie własne procesy Web/viewera i trzy exact-name kontenery ponownie
   zatrzymano; porty `18004/18005` nie nasłuchują. Zachowano DB, storage,
   cache, dwa downloady i dowody; nie wykonano cleanupu.

## Zasoby i Android

- Przed testem (`2026-09-09T16:21:49Z`): physical RAM
  `25.802/8.118 GiB total/available`; commit
  `31.629/70.802 GiB`, reserve `39.174 GiB`.
- Aktywne pagefile: C `13312 MiB`, used `522 MiB`, peak `2660 MiB`; D
  `32768 MiB`, used `677 MiB`, peak `3485 MiB`. C system-managed, D
  `32768–65536 MiB`; nie zmieniano ustawień.
- D: `854.43/931.50 GiB free/total`, osobny fizyczny NVMe od C;
  `HealthStatus=Healthy/Online`, pełny SMART `UNKNOWN`.
- Przed Web (`2026-09-09T16:32:46Z`): Windows available `8.465 GiB`, commit
  reserve `39.226 GiB`; ten sam Docker/WSL pool miał `14.313 GiB`
  `MemAvailable`, swap użyty ok. `2 MiB`.
- Po Web (`2026-09-09T17:24:30Z`): Windows available `7.398 GiB`, commit
  reserve `38.466 GiB`, lecz Docker pipe i WSL CLI nie udostępniły bieżącego
  pomiaru właściwej puli. Finalnie (`2026-09-09T17:28:47Z`) Windows available
  `7.100 GiB`, commit reserve `38.703 GiB`; metryka puli nadal niedostępna.
- Android: `RESOURCE_OBSERVABILITY_BLOCKED / NOT_RUN`. `Pixel_8` ma
  `hw.ramSize=2048`, lecz nie został uruchomiony; build, overlay, APK,
  instalacja i testowa mutacja Android = `NOT_RUN`. Nie wystąpił
  `RAM_GATE_BLOCKED` ani `COMMIT_GATE_BLOCKED` w ostatnim pomiarze.

## Wyniki i ograniczenia

- Recipe guard before/after: `FAIL-BEFORE CONFIRMED / PASS-AFTER`.
- Windows PowerShell 5.1: positive actual response PASS, negative STOP `4/4`,
  JSON array normalization `2/2`; README parser `7` bloków / `0` błędów.
- RESUME identity/DB/head/fixture: `PASS`.
- Web UI-03: klik/download/bytes/hash `PASS`; widoczna treść
  `BLOCKED_TOOLING/PARTIAL`.
- Web UI-01/02/04/05 pozostają historycznymi PASS z poprzedniej sesji i nie
  były ponawiane. `R04-A2-UI06` pozostaje
  `PARTIAL / KNOWN_DEFECT_OPEN`: transport/lista i brak duplikatu PASS,
  prawidłowy komunikat bez `LateInitializationError` FAIL.
- Android UI-01–UI-06: `NOT_RUN`.
- D-15/D-16, Qwen, embedding, KB, Visual, Temporary Chat: `NOT_RUN`.
- Kod `backend/app` i `frontend/lib`, main, rescue, produkcja, R03 oraz
  oryginalne dirty worktree: niezmienione przez C1.

## Dowody i wznowienie

- Chroniony katalog: `C:\ai-lab-core-staging\recovery\R04_A2_REAL_APP_20260909T131617Z\continuation-20260909T162252Z`.
- Lokalny manifest hashy: `docs/recovery/R04_A2_LOCAL_EVIDENCE_MANIFEST.csv`.
- Testowe poświadczenia, pobrane bajty i surowe logi pozostają `LOCAL_ONLY`;
  wartości sekretów nie weszły do Git.
- Status: `R04 A2 APP_SMOKE_PARTIAL / TEST_ONLY`; `R04 IN_PROGRESS`;
  `R03 WAITING_APPROVAL / WAITING_ESCROW_DECISION`.

Jeden następny bezpieczny krok: właściciel ocenia C1 i osobno decyduje, czy
zapewnić dozwoloną obserwowalność tej samej puli Docker/WSL oraz narzędzie
capture lokalnego viewer przed kolejnym ograniczonym Android/UI-03. STOP bez
R16, modeli, escrow, release/deploy i kolejnego pakietu.
