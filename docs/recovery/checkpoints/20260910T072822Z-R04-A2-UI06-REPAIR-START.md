# R04-20260910T072822Z-A2-UI06-REPAIR-START

- UTC: `2026-09-10T07:28:22Z`.
- Branch/worktree: `recovery/next-stabil-repair-completion` /
  `C:\ai-lab-core-recovery`.
- Parent dokumentacji i kodu: `e2c645bf99b1ad8f91d65b850ed276a49436b17a`.
- Backend zachowanego testu: `f4ea20c74f92c0423db087ba8d60bb8cc7f2ec99`.
- Poprzedni checkpoint:
  `docs/recovery/checkpoints/20260909T233459Z-R04-A2-WEBFIRST-HANDOFF.md`.

## Decyzja i dowody wejściowe

1. Właściciel przyjął WEB-FIRST jako częściowy dowód i zezwolił wyłącznie na
   naprawę/regresję `R04-A2-UI06`, powiązaną z przyszłym kryterium R16. A2,
   R04 i R16 nie są zaakceptowane; Android pozostaje
   `DEFERRED_BY_OWNER / NOT_TESTED`.
2. Recovery local/remote zaczęły na oczekiwanym parent, main `483f9bf8...` i
   rescue `5cd8f86e...` nie mają driftu. Recovery było clean, brak drugiego
   autora; oryginalny worktree zachowuje 203 wpisy i staged 0.
3. Historyczny screenshot `raw/web-ui06-disconnect-error.png` (SHA-256
   `7470163E...`) pokazuje `LateInitializationError` na stronie
   `Repozytorium dokumentów`. Screenshot odzyskania (SHA-256 `E1CD682B...`)
   pokazuje ponownie załadowaną listę. Zachowane logi Web nie zawierają stacka,
   więc screenshot dowodzi symptomu i ekranu, lecz nie samodzielnie klasy.
4. `DocumentsController` i `ClientsController` mają analogiczny wzorzec
   `late final _repository` przypisywany w `build()`. Przypięty Riverpod 3.4.2
   automatycznie invaliduje provider po zwykłym wyjątku; domyślna druga próba
   może ponownie wykonać `build()` na tej samej instancji. Najpierw trzeba
   rozstrzygnąć to deterministycznym testem prawdziwego providera.

## Jawna allowlista robocza

- `frontend/lib/features/documents/application/documents_controller.dart`;
- `frontend/test/features/documents/documents_controller_test.dart`;
- dokumentacja/checkpointy UI06 i wyłącznie niezbędne lustra statusu R04.

`ClientsController` pozostaje poza zmianą, dopóki dowód historycznego ekranu i
test lifecycle nie wykażą, że jest częścią tego samego obserwowanego defektu.
Bez zmian backendu/API/schema, modeli, Androida, produkcji, R03 i R05–R24.

Jeden następny bezpieczny krok: dodać test prawdziwego
`documentsControllerProvider`, który wymusza pierwszą porażkę transportu i
jedną kontrolowaną próbę Riverpod, a następnie zachować fail-before.

## SOURCE_PASS przed Web

- Czysty fail-before: `6` testów, `1` FAIL; stack wskazał
  `DocumentsController.build()` i drugie przypisanie `_repository`. Log
  `documents-suite-fail-before.stdout.log`, SHA-256 `03EA6C2D...`, exit `1`.
- Minimalna poprawka usuwa przechowywane `late final` i pobiera aktualny
  `documentsRepositoryProvider` przy każdym `_load()`. Nie zmieniono widoku,
  API, auth, filtrowania, paginacji ani mutacji.
- Pass-after: focused `7/7`, regresja Dokumenty/Klienci/Auth `37/37`, pełny
  Flutter `347/347`; `flutter analyze --no-pub` PASS. Pierwszy analyze został
  zatrzymany na nieużywanym imporcie i stylu nazwy ignorowanego argumentu;
  oba usunięto bez zmiany semantyki i analiza została powtórzona.
- SOURCE_PASS commit: `48fbecae0a76edb25f60e9dd314bb8d65bfbae4b`,
  opublikowany wyłącznie na recovery. Dokładnie dwa pathy: kontroler dokumentów
  i jego test. Backend nadal `f4ea20c...`.

Jeden następny bezpieczny krok: przygotować nową read-only kopię frontend ze
SOURCE_PASS, przejść bramkę zasobów i dopiero wtedy wykonać jeden Web A/B na
trzech zachowanych zasobach A2 bez migracji lub seeda.
