# R04-20260915T035527Z-D21-SINGLE-ROOT

## Decision and scope

The owner accepted R05-A4 source/API/widget evidence for source
`04ab5e58cf86896ffd946cabffde13367d343f53` and evidence HEAD
`8620871711321a42e62291e52865b5a668a4955d` as
`SOURCE_AND_API_WIDGET_TESTS_ACCEPTED / NOT_DEPLOYED`. The owner separately
authorized R04/D-21 `READ-ONLY INVENTORY / PLAN / REVIEWED DOCUMENTATION` for
`SINGLE_INSTALL_ROOT=C:\ai-lab-core` and one idempotent user start entrypoint.

No cutover, relocation, cleanup, startup implementation, task/shortcut/mount
change, deployment, service start/stop/restart, backup, restore, model, queue,
browser or application test was authorized or performed.

## Git and preservation preflight

- recovery local/tracking/remote start:
  `8620871711321a42e62291e52865b5a668a4955d`;
- `origin/main`: `483f9bf8b1a591ded8a42df5da87663c664ed5d4`;
- rescue: `5cd8f86e63e1ab829692ca2601096fd0c0d9d53a`;
- recovery was clean, staged `0`, and is 64 commits ahead of `origin/main`;
- original worktree HEAD:
  `72950657ac79b50d0afe72753632ba4cde810b95`;
- original preservation: `203/203` paths present, `203/203` SHA-256 matches,
  mismatch `0`, Git status entries `203`, staged `0`;
- R05-A4 historical WIP patch remains unchanged at SHA-256
  `81E311E7C129FC7ACC8829BC8B2E3FB31CDFEA6F800D56EE46E16E9E58A17914`.

## Bounded inventory result

The 33-row map is
`docs/recovery/R04_SINGLE_ROOT_STARTUP_MAP.csv`, 22,134 B, SHA-256
`B10E0C9E1EA6761C557EE57613DFBF8DEF435577C2B0B2FBC9C47C1311FCC0CD`.
The execution plan is `docs/recovery/R04_SINGLE_ROOT_STARTUP_PLAN.md`.

Material facts:

- active backend full ID
  `9d9b46c530412e48562b5427a0586b08c1e919ff293ec2cbd1489faffc98615a`,
  image
  `sha256:6342b36fa2cdd2501ea4e0e9fada9a9ffaa4894f0c512f19f822f009e8d63702`,
  live `/app=C:/ai-lab-core/build/deploy-main-483f9bf8/backend`, clean source
  `483f9bf8b1a591ded8a42df5da87663c664ed5d4`;
- `C:\ai-lab-core\backend` is not the live mount and materially differs from
  that deployment; it remains protected/unverified source;
- Public Gateway PID `41784` executes the main-identical in-root script;
  `/gateway-health=200`, public `/control=404`;
- Supervisor remains `INTENTIONALLY_STOPPED`; no Supervisor/worker process was
  observed and none was started;
- separate Task Scheduler logon tasks currently start Docker Desktop, Compose,
  Public Gateway, Private Gateway and Supervisor;
- per-user `NEXT-Stabil-Host.cmd` exists but its target
  `operations\runtime\start-host-services.ps1` is absent;
- the tracked Compose bootstrap has no deadline around one potentially hung
  `docker.exe info` call;
- `doc04-main-promotion` is absent, while the separately named
  `doc04a-main-promotion` is present and clean;
- promotion worktrees have no unique commits over `origin/main`, but the Visual
  V2 audit worktree has 13 unique commits and 5 dirty entries;
- external Vision worker/profile is a declared stopped Supervisor dependency
  and includes unique legacy files; `C:\Ollama-Vision-Pilot` remains
  `HOLD / UNKNOWN_CONSUMER`.

Local sanitized evidence is protected under
`C:\ai-lab-core-staging\recovery\R04_D21_SINGLE_ROOT_20260915T033112Z`:

- `sanitized-inventory.json`: 2,931 B, SHA-256
  `F4CC0F403170408C01A4CB918A75F168EEF4A984D24875D671B0BCC634DB8EAB`;
- `sanitized-consumers.json`: 3,408 B, SHA-256
  `C929E43BE055D7376A2D31FEF2B1061D90253BB066D6402FB67DF403BB72D4A0`.

No raw environment, task XML, Docker Env, cookie/profile contents, company data
or secrets were committed.

## Validation and limits

Application, Flutter, Web A/B, CORS, browser and model tests are `NOT_RUN`
because accepted source bytes were unchanged. D-21 validation is limited to
Git/preservation, directory and consumer inventory, live selected Docker
fields, current task/process identities, file hashes, CSV/JSON parsing,
reference consistency, secret scan and `git diff --check`.

The plan deliberately keeps these unresolved gates visible: current schedule
destination values were not read from business DB; external worker session
migration is untested; `C:\Ollama-Vision-Pilot` consumer remains unknown;
staging is a mixed protected tree. These gaps block their corresponding
cutover/cleanup actions, not review of the prepared map.

## Status and next step

- `R05 A4 SOURCE_AND_API_WIDGET_TESTS_ACCEPTED / NOT_DEPLOYED`;
- `R04 D21 SINGLE_ROOT_STARTUP_MAP_READY_FOR_REVIEW / NO_OPERATIONAL_CHANGES`;
- cutover / relocation / cleanup / startup implementation: `NOT_RUN`;
- `R04 IN_PROGRESS`;
- `R05 IN_PROGRESS — OPERATOR_WEB_RUNTIME_AND_EXTERNAL_END_TO_END_NOT_VERIFIED`;
- `R03 WAITING_APPROVAL / WAITING_ESCROW_DECISION`;
- `NEXT_FEATURE_STEPS BLOCKED_PENDING_R04_SINGLE_ROOT_AND_STARTUP`.

One next safe step: owner review of the map and a separate decision on the
first minimal `D21-P1` source/test package: the missing tracked host launcher,
bounded Docker CLI timeout in the existing Compose helper, and offline
idempotency/identity tests. This checkpoint does not authorize D21-P1 or any
operational step.
