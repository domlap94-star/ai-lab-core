# R04-20260909T192356Z-A2-C2-HANDOFF

- UTC: `2026-09-09T19:23:56Z`.
- Branch/worktree: `recovery/next-stabil-repair-completion` /
  `C:\ai-lab-core-recovery`.
- Parent dokumentacji: `e63ed05c6a9a31cb3657215dba548076e7f8297c`.
- Code under test: `f4ea20c74f92c0423db087ba8d60bb8cc7f2ec99`;
  archive SHA-256 `B93DF8FE6F2D86EE32394D7DE08EF45DB44E99A97C9A38F6231A44200400F391`.
- Poprzedni checkpoint:
  `docs/recovery/checkpoints/20260909T184535Z-R04-A2-C2-START.md`.
- Zakres zgody: wyłącznie C2 — jedna diagnoza dostępu do telemetrii, ten sam
  pool Docker/WSL, istniejący UI-03 download i jeden Android build/UI po
  bramkach. Bez zmian produktu, modeli, produkcji, UI-06 fix, escrow i dalszych
  pakietów.

## Zakończone

1. Preflight Git i zasobów PASS: recovery local/remote zaczęły na `e63ed05c...`,
   main/rescue bez driftu, oryginalny worktree nadal 203 wpisy i staged 0.
2. Docker denial rozstrzygnięty: ten sam normalny użytkownik/session,
   `docker.exe` i `desktop-linux`; zwykły proces dochodzi do named pipe, gdzie
   sandbox go odrzuca (exit 1), a identyczny read-only odczyt przez wąski
   approval działa (exit 0). Bez admina, zmiany pipe/context/hosta.
3. Engine `78446bed...`, WSL2 kernel i boot ID powiązano z jedynym własnym
   kontenerem telemetrycznym. Trzy próbki około 10 s: pool MemAvailable
   `14.312/14.300/14.302 GiB`, swap free `7.998 GiB`; MemTotal `17.563 GiB`
   zgadza się z engine i jest odrębny od 64 MiB cgroup.
4. Helper PowerShell 5.1: parser PASS, realny read PASS, zły pełny ID odmowa
   PASS, invariant CSV 10 kolumn PASS. Pierwszy błąd quoting/locale helpera był
   błędem harnessu i został poprawiony przed monitorowanym obciążeniem.
5. Stos A2 wznowiono bez migracji/seeda. Pełne ID, DB
   `ai_lab_r04_a2_20260909`, head, 2/2 klientów, 1/1 dokument 204 B i
   `/version` source revision `f4ea20c...` przeszły.
6. Build-only patch apply/reverse PASS; przed overlay 316/316 tracked plików
   odpowiadało source, po overlay różnił się tylko
   `android/app/build.gradle.kts`. Package `pl.ailab.app.r04test`, produkcyjny
   key.properties nieczytany, pubspec.lock bez zmian, offline pub get exit 0.
7. Jedyny build osiągnął `assembleDebug`. Z 24 świeżych próbek Windows RAM
   spadł do `3.889 GiB`, przy commit reserve `34.636 GiB` i pool available
   `13.852 GiB`; klasyfikacja `RAM_GATE_BLOCKED`. Build przerwano, bez retry,
   APK, instalacji i startu emulatora. Android UI-01–06 = `NOT_RUN`.
8. UI-03 download C1 nadal ma 204 B i SHA `C0EBC642...`; nie uruchamiano Web
   ani trzeciego downloadu. Brak dozwolonego narzędzia capture okna desktopu:
   `WAITING_OWNER_VISUAL_EVIDENCE`, nie FAIL produktu.
9. Trzy kontenery A2 zatrzymano; telemetry stop/remove po exact ID/owner/run;
   porty `18004/18005` puste, AVD nieuruchomiony. Wolumen/DB/storage/downloady,
   android cache/partial build i dowody zachowane.

## Wyniki i granice

- C2: `APP_SMOKE_PARTIAL / TEST_ONLY`.
- Telemetria: `POOL_OBSERVED`; `RESOURCE_OBSERVABILITY_BLOCKED` zamknięte dla
  tej sesji.
- Android: `RAM_GATE_BLOCKED`; build exit 1 po kontrolowanym przerwaniu,
  APK/install/emulator/UI = `NOT_RUN`.
- Web UI-03: click/download/hash zachowane PASS; widoczna treść
  `WAITING_OWNER_VISUAL_EVIDENCE`.
- Web UI-01/02/04/05 pozostają historycznymi PASS z wcześniejszego A2.
  `R04-A2-UI06` pozostaje `PARTIAL / KNOWN_DEFECT_OPEN_R16`.
- D-15/D-16, Qwen, embedding, KB, Visual, Temporary Chat: `NOT_RUN`.
- Produkcyjne business writes, kolejki, Qdrant, Gmail, backup/snapshot, deploy,
  rescue adoption i escrow: `0` działań C2.
- Dowody `LOCAL_ONLY`:
  `C:\ai-lab-core-staging\recovery\R04_A2_REAL_APP_20260909T131617Z\continuation-c2-20260909T184535Z`.

Jeden następny bezpieczny krok: właściciel otwiera
`C:\ai-lab-core-staging\recovery\R04_A2_REAL_APP_20260909T131617Z\continuation-20260909T162252Z\web-ui03-download-1.txt`
w lokalnym viewerze i przekazuje screenshot wyłącznie tego okna. Ponowienie
Android wymaga odrębnej decyzji i okna zasobowego; bez automatycznego retry,
R16, modeli, escrow, release/deploy i dalszego R04.
