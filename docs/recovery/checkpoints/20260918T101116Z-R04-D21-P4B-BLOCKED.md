# R04 / D-21 / P4-B — Phase B blocked after safe partial mutation

UTC evidence window: `2026-09-18T09:52:16Z–2026-09-18T10:11:16Z`

OP_ID: `R04-D21-P4B-WINDOW-20260918T084652Z`

Published Phase-A parent: `bd9acc14f6f0d12bacdb5a9a8dd5a8f69a5b333b`

## Result

The owner supplied the exact single-use Phase-B authorization. Fresh drift checks
passed for the six container identities, PostgreSQL health, the five task
preimages, the Startup wrapper and the bounded HTTP surface. Phase B then made
one safe first mutation: the exact `NEXT-Stabil-Host.cmd` was moved out of the
Startup folder to the protected rollback directory. Its SHA-256 remained
`C843C05CB023CE187D7C6829DB904E2FDB58B893C8072CBC33E476D7307C06D4`.

Task import exposed two installation-transport facts without changing the
approved XML files: decoded XML strings require removal of the encoding
declaration in memory, and the proposed Host principal `domai` resolves to the
same SID already used by the five accepted task preimages,
`S-1-5-21-712169069-4165966233-3173903118-1001`. The new Host task was created
as disabled, with no trigger, `InteractiveToken`, `LeastPrivilege`, `IgnoreNew`
and `PT15M`.

Updating the first existing task (`NEXT Stabil - Docker Desktop`) then failed
with `Access denied` because the current process token was not elevated. One
formal `RunAs` attempt was made; the Windows UAC operation was cancelled by the
user or system. It was not retried and no alternate channel was used.

## Exact state at STOP

- `NEXT Stabil - Host`: exists, disabled, no trigger; never run.
- Startup wrapper: absent from Startup, exact rollback copy preserved.
- Docker Desktop, Docker Compose, Public Gateway, Private Gateway and
  Supervisor task definitions: unchanged from their Phase-A preimages; their
  previous logon triggers therefore remain present.
- Launcher and runtime files: not installed.
- Global startup manifest: not installed; repository draft remains
  `NOT_APPROVED`.
- Existing helper remains SHA-256
  `445AFFC04CF916602A30FDC9BBE0C8557E31AC670E8C5D389636BAB11E6DECC5`.
- Warm runs: `0/2`; Host activation/logon trigger: not performed.
- Tasks/services started by this window: `0`; containers mutated: `0`;
  Supervisor remained `INTENTIONALLY_STOPPED`.

The permitted SAFE_INACTIVE normalization was attempted once after the access
denial but could not update the protected existing tasks without elevation.
The operation therefore stops as
`P4B PARTIAL_SAFE_INACTIVE / INSTALLATION_BLOCKED_UAC_CANCELLED`, not as an
installation or startup PASS. The legacy triggers were not newly enabled by
this window, but they also were not removed; this distinction is material.

## Local-only evidence

Protected execution root:
`C:\ai-lab-core-staging\recovery\R04_D21_P2_20260915T212340Z\p4-startup-activation-20260917T210051Z\p4b-window-20260918T084652Z\phase-b-execution-20260918T095136Z`

- pre-mutation drift projection SHA-256:
  `D98EA2147D27EEE0F0FAD0EEA0740C2E6624EE990BB347A240FE0A77CC795773`;
- safe blocker projection SHA-256:
  `3279700CBD0584FFD7D03DF66B92F390C850714B9469ED99F8318B401D239C13`;
- final execution marker SHA-256:
  `BAEDEDEE251507C7C1BC27EAC42DD11D5DAB12C860CE6DBCFD067C253286BF3`.

Raw logs, XML and rollback bytes remain LOCAL_ONLY. No secrets, full inspect,
business data or task XML are committed.

## Resume gate

The exact Phase-B authorization is consumed. Any continuation requires a new
current owner decision with the owner present to accept one UAC prompt, followed
by a fresh bounded drift check. Do not install files, run the launcher, enable
Host, or perform warm runs before that decision. Reboot/logoff/cold start,
Supervisor, backup/restore, relocation, P5 and R06 remain outside scope.
