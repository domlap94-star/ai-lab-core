# R04-20260910T152711Z-A2-UI06-OWNER-HANDOFF

- UTC: `2026-09-10T15:27:11Z`.
- Branch/worktree: `recovery/next-stabil-repair-completion` /
  `C:\ai-lab-core-recovery`.
- Start doc HEAD: `e7e7591df13e989f4e67720e13212f344a8c95c6`.
- Start checkpoint commit: `8f6cd32c503cd7196dfb7fdb6f9fb91263ba8f4b`.
- Accepted frontend source/test:
  `48fbecae0a76edb25f60e9dd314bb8d65bfbae4b`.
- Saved synthetic backend source:
  `f4ea20c74f92c0423db087ba8d60bb8cc7f2ec99`.
- Previous checkpoint:
  `docs/recovery/checkpoints/20260910T104603Z-R04-A2-UI06-OWNER-START.md`.

## Rzeczywisty wynik

Obserwator: `OWNER_OPERATED / OWNER_OBSERVED` przez istniejący RustDesk.
Codex nie sterował RustDesk ani browserem.

| Scenariusz | Wynik | Dowód |
|---|---|---|
| B — odczyt po utracie połączenia | `PASS / OWNER_OPERATED_RUSTDESK` | Przy zatrzymanym dokładnym backendzie widoczny był ograniczony komunikat błędu. Po uruchomieniu tego samego ID i jednym retry wróciła jedna właściwa pozycja `synthetic-document.txt`, bez wewnętrznego wyjątku. Dwa świadomie przekazane screenshoty są zhashowane w lokalnym manifeście. |
| A — pierwszy odczyt przy backend down | `NOT_VERIFIED / PREFETCH_CONTAMINATED` | Po zamknięciu starych kart, jednym restarcie wyłącznie Web i świeżym logowaniu klient wykonał `GET /api/v1/documents` o `2026-09-10T15:20:34.298643639Z`, zanim właściciel otworzył repozytorium. Drugiego outage nie wykonano. |

Wcześniejsza pierwsza sesja także miała odczyty listy/contentu/szczegółu przed
potwierdzeniem warunku A, dlatego jej pierwszy backend outage sklasyfikowano
wyłącznie jako B. Biały ekran świeżej sesji zachowano na screenshotcie i
wykonano jeden kontrolowany restart tylko Web; po nim login działał.

## Zasoby i ograniczenia

- Monitor: `998` próbek PASS w kilku ograniczonych oknach; minima Windows
  `4.750 GiB`, commit reserve `32.620 GiB`, pool `13.790 GiB`; swap maksymalnie
  `1.9 MiB`. Jedna końcowa linia `DOCKER_READ_FAILED` nastąpiła po zamierzonym
  zatrzymaniu telemetrii.
- Sesja przekroczyła 25 minut i miała przerwy świeżego pomiaru >30 s podczas
  oczekiwania na operatora. Zasoby nie zostały automatycznie zatrzymane po
  pięciu minutach. To ogranicza proceduralny PASS i pozostaje jawne.
- Wykonano jeden stop/start testowego backendu dla B oraz jeden restart Web po
  białym ekranie. A nie otrzymało outage. Testy aplikacyjne `NOT_RUN`, bo
  source/test nie zmieniono.
- Preflight: syntetyczna DB `ai_lab_r04_a2_20260909`, head
  `followup_assistant_chat_history_20260829`, 2 klientów i 1 dokument. Logi
  próby nie zawierały biznesowego POST/PATCH/DELETE. Końcowy read-only SQL
  został wykonany przed shutdownem, ale jego stdout nie został zwrócony przez
  proces orkiestrujący, więc nie stanowi niezależnego końcowego countu.
- Flutter przy pierwszym starcie wykonał standardowe dependency resolution;
  nie zmienił `pubspec.lock` ani kontrolera. Drugi start użył `--no-pub`.
- Trzy zachowane kontenery A2 są `exited`; Web zatrzymany; telemetry
  `5d45b3de...` usunięta po kontroli; porty `18004/18005` wolne. DB/volume,
  storage, cache, APK i dowody są zachowane `LOCAL_ONLY`.
- Produkcja, R03, modele, KB, Vision, Temporary Chat, Qdrant, Gmail, n8n,
  kolejki, migracje, backup, escrow, main i rescue: bez działań tej sesji.

## Handoff

Status: `R04-A2 UI06 SOURCE_ACCEPTED / WEB_AB_PARTIAL / TEST_ONLY`;
A `NOT_VERIFIED`, B `PASS`; `UI_OBSERVER=OWNER_OPERATED_RUSTDESK`;
`NOT_DEPLOYED`. R04 pozostaje `IN_PROGRESS`; R03 pozostaje
`WAITING_APPROVAL / WAITING_ESCROW_DECISION`; Android
`DEFERRED_BY_OWNER / NOT_TESTED`; D-15/D-16 AI acceptance `NOT_RUN`.

Jeden następny bezpieczny krok: właściciel i ChatGPT oceniają częściowy dowód
i osobno decydują, czy A wymaga nowej deterministycznej ścieżki testowej
eliminującej prefetch. Nie uruchamiać jej automatycznie ani nie zaczynać
`ClientsController`, Androida, R16, R05–R24, deploymentu lub kolejnej naprawy.
